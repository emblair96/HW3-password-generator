User will click on the generate password button
User will be met with a series of prompts regarding their password
The password length quesiton is unique => this number should be used as the multiplier in some form of Math.random/math.floor?  
If user hits OK for lowercase, uppercase, numbers, or special characters, === true
If user hits cancel for lowercase, uppercase, numbers, or special characters === false
If the value is true, then the password should include those elements
If the value is false, then the password should not include those elements
The password generator should have multiple if else options depending on the users response to the questions

